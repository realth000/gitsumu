name: gitsumu
description: Dart package provides code about compile environment (git info, flutter version ...).
version: 0.0.1
repository: https://github.com/realth000/gitsumu

environment:
  sdk: ^3.1.5

# Add regular dependencies here.
dependencies:
  analyzer: ^6.2.0
  build: ^2.4.1
  collection: ^1.18.0
  source_gen: ^1.4.0
  path: ^1.8.0
  args: ^2.4.2
  build_config: ^1.1.1
  yaml: ^3.1.2

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0




