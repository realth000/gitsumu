name: gitsumu
description: Dart package generates git info.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.5

# Add regular dependencies here.
dependencies:
  analyzer: ^6.3.0
  build: ^2.4.1
  source_gen: ^1.4.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
